using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActorStatManager : MonoBehaviour {

	/*Bar Width = 75f

  Shield Pip Prefab;
  Health Pip Prefab;

  Health Pip Width = BarWidth / Health;
  Shield Pip Width = BarWidth / Shield;

  ShieldOriginPos = 60, 45
  HealthOriginPos = 60, 50

  Create Shield & Health Bars

  Set Health(delta)

  Set Shield(delta)*/

}
